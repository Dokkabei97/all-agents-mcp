# Task Completion Checklist

When a task is completed, run:
1. `npm run build` — Ensure TypeScript compiles without errors
2. `npm run lint` — Ensure Biome passes (auto-fixes applied)
3. `npm test` — Ensure all tests pass

All three must pass before considering a task done.
